The following program is a database file for a Lego Store.
In order to run the Lego store make sure all files are in the same directory.

This program requires that your device can perform task in Python and SQL.

Execute the program by running py main.py in the command prompt.
This will run the code and create databases and tables. 